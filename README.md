# Loopback Test App

A minimal test application for E2E testing the Loopback feedback auto-fix pipeline.

**Known bug:** Dark mode toggle doesn't persist across page reloads (localStorage key mismatch).
